#
# Postgresql puppet module

Postgresql custom datadir is "<%= @data_directory %>"

This value is present in postgresql.conf
The default postgresql dir is renamed as main.orig, 
in the default location "<%= @postgresql_default_dir %>"